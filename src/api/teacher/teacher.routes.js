const router = require("express").Router();
const teacherController = require('./teacher.controller')

// router.route("/").post(teacherController.create)
// router.route("/").get(teacherController.list)
// router.route("/:teacherId").get(teacherController.show)
// router.route("/:teacherId").put(teacherController.update)
// router.route("/:teacherId").delete(teacherController.destroy)

module.exports = router;
